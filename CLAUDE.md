# ルール
* OSSをインストールする場合、latestではなく安定バージョンを明示的に指定して使用すること。
* コンテナランタイムはDockerではなくpodmanを使用すること。
* npxはローカル（package.json）にインストール済みのパッケージに対してのみ使用すること。ローカルにないパッケージをnpxで直接実行してはならない。必要な場合は先にnpm installで安定バージョンを指定してインストールしてからnpxで使うこと。
