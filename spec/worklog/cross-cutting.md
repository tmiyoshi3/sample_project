# 作業ログ: 横断的設計（アーキテクチャ設計）

## 2026-06-02: Phase 7 - アーキテクチャ基本設計（9回目: F-04-02 差分更新）

### 実施内容
- F-04-02（サプライヤー詳細画面）のUI仕様・API仕様・ユースケースを確認し、既存の設計方針で対応できない新パターンを特定
- spec/architecture.md を差分更新（1セクション追加、既存2セクション更新、5設計判断追加、共通コンポーネント2項目追加、未確認事項4件追加）

### 新パターン分析結果
| パターン種別 | 確認済みパターン | 対応 |
|---|---|---|
| タブ内CRUD操作 | 詳細画面のタブ内でサブリソースのCRUDをモーダルベースで完結。契約タブ・認証タブは完全CRUD、製品タブは追加・削除、評価タブは追加のみ | 詳細画面タブ方針に「タブ内CRUD操作方針」を追記 |
| スコアリング入力（レンジスライダー） | 4カテゴリ（品質/納期/価格/対応）のスコアをレンジスライダー（min=1, max=5, step=0.5）で入力 | スコアリング入力・可視化方針セクション追加、app-range-slider 共通コンポーネント候補追加 |
| スコア可視化（水平バー） | 各評価エントリの4カテゴリスコアを水平バー（幅 = value * 20%）で表示 | スコアリング入力・可視化方針セクション追加、app-score-bar 共通コンポーネント候補追加 |

### 既存パターンで対応済み（変更不要）
| パターン種別 | 対応状況 |
|---|---|
| タブ付き詳細画面 | 詳細画面タブ方針（F-03-02で策定）で対応済み。5タブ構成は既存パターンの範囲内 |
| サブリソースAPI | サブリソースAPI方針（F-03-02で策定）で対応済み。所有権チェック方針を追記 |
| 星評価表示 | 星評価表示方針（F-04-01で策定）で対応済み |
| ステータスバッジ | app-status-badgeで対応済み。認証ステータス（VALID/EXPIRED/EXPIRING_SOON/REVOKED）は新しいEnum値だが方針変更不要 |
| カード形式表示 | カードリスト方針（F-03-04で策定）で対応済み。連絡先カード・認証カードは既存パターンの範囲内 |
| 確認ダイアログ付き削除 | app-confirm-dialogで対応済み |
| 空状態表示 | app-empty-stateで対応済み |
| 論理削除 | 削除戦略で対応済み。サプライヤーはビジネスエンティティのため論理削除（INACTIVE化） |

### 主要な設計判断（追加: 5項目）
1. タブ内CRUD方式: タブ内モーダルCRUD — 親詳細画面のコンテキスト内でサブリソースを管理。画面遷移不要
2. スコア入力方式: レンジスライダー — 1.0〜5.0のstep=0.5で直感的入力、バリデーション負荷低
3. スコア可視化方式: 用途で使い分け — 総合スコアは星表示、カテゴリ別内訳は水平バー
4. サブリソース所有権チェック: APIレイヤで検証 — 更新・削除時にサブリソースが親に属するか確認し不一致で404
5. タブ種別使い分け: 表示専用/CRUDタブ/ハイブリッドの3種類を定義

### 追加したセクション（1件）
1. FE構成 > スコアリング入力・可視化方針（新規）

### 更新した既存セクション（2件）
1. FE構成 > 詳細画面タブ方針 — 「タブ内CRUD操作方針」「タブ種別の使い分け」テーブルを追記
2. BE構成 > サブリソースAPI方針 — 所有権チェック方針、CRUD対応サブリソースのエンドポイント命名規則を追記

### 追加した共通コンポーネント候補（2件）
- FE: app-range-slider（レンジスライダー入力、min/max/step設定可、FormControl対応）
- FE: app-score-bar（水平スコアバー、0.0〜5.0、ラベル+数値表示）

### 更新ファイル
| カテゴリ | 件数 |
|---|---|
| アーキテクチャ設計 | 1（spec/architecture.md 差分更新） |
| ワークログ | 2（spec/worklog/cross-cutting.md 更新、spec/worklog/index.md 更新） |
| トレーサビリティ | 1（spec/traceability/index.md 最終更新日更新） |

### 未確認事項
- サプライヤー詳細画面のロール制限（全CRUD操作に@RolesAllowed未設定）
- サプライヤー評価の編集・削除機能の要否（現行は追加のみ）
- evaluatedByフィールドのユーザー名解決方針
- 全タブ同時ロードの移行後対応（lazy load方針適用確認）

### 次に実行すべきskill
- `08-test-data-design`
- next target feature ID: F-04-02-001
- current 中分類: F-04-02
- 中分類進捗: 10/10 機能ID usecase-linked（アーキテクチャ設計完了）
- reason: F-04-02のアーキテクチャ設計差分更新完了。次はテストデータ設計

---

## 2026-06-01: Phase 7 - アーキテクチャ基本設計（8回目: F-04-01 差分更新）

### 実施内容
- F-04-01（サプライヤー一覧画面）のUI仕様・API仕様・ユースケースを確認し、既存の設計方針で対応できない新パターンを特定
- spec/architecture.md を差分更新（2セクション追加、4設計判断追加、共通コンポーネント1項目追加、既存セクション1箇所更新）

### 新パターン分析結果
| パターン種別 | 確認済みパターン | 対応 |
|---|---|---|
| 比較選択パターン | チェックボックス最大3件選択→比較ボタン（件数表示・2件以上でenabled）→クエリパラメータで比較画面遷移→比較API | 比較選択・比較画面方針セクション追加 |
| 星評価表示 | 星5つ + 数値（☆☆☆☆☆ 0.0）。SupplierRatingの4項目overallScoreの平均値（0.0〜5.0） | 星評価（スコアリング）表示方針セクション追加、app-star-rating 共通コンポーネント候補追加 |

### 既存パターンで対応済み（変更不要）
| パターン種別 | 対応状況 |
|---|---|
| 一覧画面+マルチフィルタ | 一覧画面フィルタ方針（F-03-01で策定）で対応済み。評価フィルタ（数値範囲系）のみ方針に追記 |
| クライアントサイドフィルタ→サーバーサイド | 既存方針で対応済み（技術的負債解消） |
| ステータス値の三重不一致 | ステータスラベルの正規化方針で対応済み |
| ページネーションUI未実装 | app-data-table方針で対応済み |
| 行クリック→詳細遷移 | 標準パターン |
| 新規登録ボタン→新規画面遷移 | 標準パターン |

### 主要な設計判断（追加: 4項目）
1. 比較選択UI: チェックボックス分離 — 行クリック（詳細遷移）と選択（比較用）を混同しない
2. 比較画面遷移: 専用ページ遷移（クエリパラメータ） — データ量が多くモーダルでは収まらない
3. 星評価表示: 星アイコン+数値 — 視覚的スキャナビリティ向上、半星対応で0.5単位の評価差を表現
4. サプライヤーフィルタ移行: サーバーサイド移行 — BE側にsearchByName()やstatusパラメータが既存。クライアントサイドは技術的負債

### 追加したセクション（2件）
1. FE構成 > 比較選択・比較画面方針（新規）
2. FE構成 > 星評価（スコアリング）表示方針（新規）

### 更新した既存セクション（1件）
1. FE構成 > 一覧画面フィルタ方針 — 数値範囲系フィルタ（最低評価フィルタ）の方針を追記

### 追加した共通コンポーネント候補（1件）
- FE: app-star-rating（星評価表示、0.0〜5.0対応、半星・数値併記）

### 更新ファイル
| カテゴリ | 件数 |
|---|---|
| アーキテクチャ設計 | 1（spec/architecture.md 差分更新） |
| ワークログ | 2（spec/worklog/cross-cutting.md 更新、spec/worklog/index.md 更新） |
| トレーサビリティ | 1（spec/traceability/index.md 最終更新日更新） |

### 未確認事項
- サプライヤー比較画面の詳細表示内容（ローディング中で完全確認できず）
- サプライヤー管理のロール制限（現行@RolesAllowed未設定）
- サプライヤーステータス値の三重不一致の正規化詳細
- email/phone空問題の解消方針
- 星評価の入力（評価登録）UIの要否
- 比較APIのN+1クエリ解消・currentRating null問題

### 次に実行すべきskill
- `08-test-data-design`
- next target feature ID: F-04-01-001
- current 中分類: F-04-01
- 中分類進捗: 8/8 機能ID usecase-linked（アーキテクチャ設計完了）
- reason: F-04-01のアーキテクチャ設計差分更新完了。次はテストデータ設計

---

## 2026-05-26: Phase 7 - アーキテクチャ基本設計（7回目: F-03-05 差分更新）

### 実施内容
- F-03-05（製品新規登録/編集画面）のUI仕様・API仕様・ユースケースを確認し、既存の設計方針で対応できない新パターンを特定
- spec/architecture.md を差分更新（4セクション追加、4設計判断追加、共通コンポーネント2項目追加、既存セクション1箇所更新）

### 新パターン分析結果
| パターン種別 | 確認済みパターン | 対応 |
|---|---|---|
| ウィザード（マルチステップフォーム） | 5ステップウィザード（基本情報→価格/在庫→仕様→画像/ドキュメント→確認）。ステップごとにバリデーション、順次遷移、データ保持 | ウィザード方針セクション追加、app-wizard 共通コンポーネント候補追加 |
| 非同期フィールドバリデーション | SKUリアルタイム重複チェック（debounce 300ms、ステータスインジケーター: idle/checking/available/duplicate） | 非同期フィールドバリデーション方針セクション追加、asyncUniqueValidator 共通処理追加 |
| ファイルアップロードワークフロー | Deferred（新規作成: forkJoin並列アップロード）vs Immediate（編集: 即時API呼び出し）の2パターン | ファイルアップロードワークフロー方針セクション追加 |
| 作成/編集フォーム共通化 | 新規作成（ウィザード）と編集（単一ページ）でFormGroup定義の約90%が重複。UI構成は異なるがフォーム定義を共有サービスに抽出可能 | 作成/編集フォーム共通化方針セクション追加 |

### 主要な設計判断（追加: 4項目）
1. ウィザード vs 単一ページフォーム: 使い分け — 新規作成はウィザード（多項目・ガイダンス）、編集は単一ページ（直接アクセス）
2. 非同期フィールドバリデーション: AsyncValidator + debounce(300ms) — SKU重複チェックのリアルタイムフィードバック
3. ファイルアップロード戦略: Deferred（親ID未存在） vs Immediate（親ID存在）の使い分け
4. 作成/編集フォーム共通化: FormGroup定義を共有サービスに抽出 — UI構成が異なるためコンポーネント共用は不可

### 追加したセクション（4件）
1. FE構成 > ウィザード（マルチステップフォーム）方針（新規）
2. FE構成 > 作成/編集フォーム共通化方針（新規）
3. FE構成 > 非同期フィールドバリデーション方針（新規）
4. FE構成 > ファイルアップロードワークフロー方針（新規）

### 更新した既存セクション（1件）
1. FE構成 > ディレクトリ構成 > validators/ — async-validators.ts を追加

### 追加した共通コンポーネント候補（2件）
- FE: app-wizard（ウィザードコンテナ + ステップインジケーター）
- FE共通処理: asyncUniqueValidator（非同期一意性チェックバリデーターファクトリ）

### 更新ファイル
| カテゴリ | 件数 |
|---|---|
| アーキテクチャ設計 | 1（spec/architecture.md 差分更新） |
| ワークログ | 2（spec/worklog/cross-cutting.md 更新、spec/worklog/index.md 更新） |
| トレーサビリティ | 1（spec/traceability/index.md 最終更新日更新） |

### 未確認事項
- 製品登録成功後のリダイレクト先（製品一覧 or 製品詳細）
- 製品登録後の画像/ドキュメント部分アップロード失敗時のリカバリーUX
- 製品登録/更新のロール制限（現行は@RolesAllowed未設定）
- ファイルアップロードのサイズ制限（現行はフロント/バックエンドともに制限なし）
- 他の新規作成画面のウィザード化要否

### 次に実行すべきskill
- `08-test-data-design`
- next target feature ID: F-03-05
- current 中分類: F-03-05
- 中分類進捗: 5/5 中分類のアーキテクチャ設計完了（F-03-01〜F-03-05）
- reason: F-03-05のアーキテクチャ設計差分更新完了。次はテストデータ設計

---

## 2026-05-26: Phase 7 - アーキテクチャ基本設計（6回目: F-03-04 差分更新）

### 実施内容
- F-03-04（バンドル管理画面）のUI仕様・API仕様・ユースケースを確認し、既存の設計方針で対応できない新パターンを特定
- spec/architecture.md を差分更新（4セクション追加/更新、4設計判断追加、共通コンポーネント1項目追加、既存方針1箇所更新）

### 新パターン分析結果
| パターン種別 | 確認済みパターン | 対応 |
|---|---|---|
| カードリスト表示 | テーブルではなくカード形式で少数データをリッチ表示（バンドル名・構成製品チップ・価格情報） | カードリスト方針セクション追加（テーブル/カードの選択基準を明確化） |
| インラインCRUDトグル型 | 一覧表示⇔フォーム表示の排他切替。F-03-03の2カラム型とは異なるバリエーション | 管理画面（インラインCRUD）方針を更新（2カラム型/トグル型の2バリエーション定義） |
| オートコンプリート検索 | 製品検索テキストボックス→ドロップダウン候補→クリック追加。現行はFEフィルタ（size=100制限、技術的負債） | オートコンプリート方針セクション追加（サーバーサイド検索APIに移行） |
| 派生フィールド二重計算 | バンドル価格がFE（Math.floor、プレビュー用）とBE（RoundingMode.FLOOR、保存用）で計算 | 派生フィールドの計算方針セクション追加 |
| バンドルコード自動生成 | BDL-{timestamp}。カテゴリコードと同パターン | エンティティコード自動生成方針を更新（BDLプレフィックス追加、3文字プレフィックスで統一） |

### 主要な設計判断（追加: 4項目）
1. 一覧表示方式: テーブル vs カード使い分け — テーブルは件数多・ページング・ソート必要時、カードは件数少・リッチ情報表示時
2. 管理画面レイアウト: 2カラム型 / トグル型の2種併用 — フォームサイズと一覧参照の必要性で使い分け
3. オートコンプリートデータソース: サーバーサイド検索API — クライアントサイド事前取得の100件制限を解消
4. 派生フィールド計算: FE+BE二重計算 — 単純な四則演算では許容。端数処理はFE/BE統一

### 追加/更新したセクション（4件）
1. FE構成 > 管理画面（インラインCRUD）方針 — 2バリエーション（2カラム型/トグル型）に拡張
2. FE構成 > カードリスト方針（新規）
3. FE構成 > オートコンプリート（検索選択）方針（新規）
4. FE構成 > 派生フィールドの計算方針（新規）

### 更新した既存セクション（1件）
1. BE構成 > エンティティコード自動生成方針 — BDLプレフィックス追加、3文字プレフィックス統一ルールに更新

### 追加した共通コンポーネント候補（1件）
- FE: app-autocomplete（検索選択UI、debounce付きテキスト入力→ドロップダウン候補→クリック選択）

### 更新ファイル
| カテゴリ | 件数 |
|---|---|
| アーキテクチャ設計 | 1（spec/architecture.md 差分更新） |
| ワークログ | 2（spec/worklog/cross-cutting.md 更新、spec/worklog/index.md 更新） |
| トレーサビリティ | 1（spec/traceability/index.md 最終更新日更新） |

### 未確認事項
- バンドル管理のロール制限（現行は@RolesAllowed未設定。ADMIN/MANAGERのみCRUD可能とするのが妥当）
- バンドル有効期間（valid_from/valid_until）の自動ステータス変更機能の要否
- バンドル価格の整合性（totalPrice再計算 vs bundlePrice DB保存値の乖離対応）
- バンドル構成製品のsort_order（DB上にカラムあるがFE未使用。表示順序の保証要否）

### 次に実行すべきskill
- `08-test-data-design`
- next target feature ID: F-03-04
- current 中分類: F-03-04
- 中分類進捗: 4/5 中分類完了（F-03-01〜F-03-03 done、F-03-04はアーキテクチャ設計完了。F-03-05は04-ui-specificationから）
- reason: F-03-04のアーキテクチャ設計差分更新完了。次はテストデータ設計

---

## 2026-05-25: Phase 7 - アーキテクチャ基本設計（5回目: F-03-03 差分更新）

### 実施内容
- F-03-03（カテゴリ管理画面）のUI仕様・API仕様・ユースケースを確認し、既存の設計方針で対応できない新パターンを特定
- spec/architecture.md を差分更新（3セクション追加、6設計判断追加、共通コンポーネント1項目追加、既存方針1箇所修正）

### 新パターン分析結果
| パターン種別 | 確認済みパターン | 対応 |
|---|---|---|
| 階層ツリー表示 | フラットリスト（parentId）→FE側buildTree()→展開/折りたたみUI | 階層データ（ツリー）表示方針セクション追加、app-tree-view 共通コンポーネント候補追加 |
| インラインCRUD管理画面 | 左:ツリー/リスト + 右:フォームパネルの2カラム構成、画面遷移なし | 管理画面（インラインCRUD）方針セクション追加 |
| 物理削除 | カテゴリは物理削除（em.remove）。既存の「物理削除は行わない」ルールと矛盾 | スキーマ設計方針の削除戦略を修正（論理削除=原則、物理削除=分類/マスターデータ系の例外） |
| 削除保護（409 Conflict） | 子カテゴリ・紐付き製品の存在チェック → 409 Conflict | 削除保護方針セクション追加 |
| 循環参照防止 | 自己参照FK（parent_id）の循環をFE/BEともに未検証（技術的負債） | 自己参照エンティティのバリデーション方針セクション追加 |
| エンティティコード自動生成 | CAT-{currentTimeMillis}（衝突リスクあり、技術的負債） | エンティティコード自動生成方針セクション追加（DBシーケンスベースに変更） |

### 主要な設計判断（追加: 6項目）
1. 削除戦略: ビジネスエンティティ=論理削除、分類/マスターデータ系=物理削除（参照整合性チェック付き）
2. 階層ツリーデータ管理: FE側buildTree() — BE側ツリー構築は複雑でキャッシュ柔軟性が低い
3. エンティティコード生成: DBシーケンスベース — タイムスタンプの衝突リスクを排除
4. 循環参照防止: BEバリデーション — 祖先チェーン走査で検出し422を返す
5. 管理画面レイアウト: インラインCRUD（2カラム） — マスターデータはデータ量少・操作頻度高で画面遷移不要
6. サプライヤータブAPI: （既存判断に包含されるため新規追加なし）

### 追加したセクション（3件）
1. FE構成 > 階層データ（ツリー）表示方針
2. FE構成 > 管理画面（インラインCRUD）方針
3. BE構成 > 削除保護方針 + 自己参照エンティティのバリデーション方針 + エンティティコード自動生成方針

### 修正した既存セクション（1件）
1. DB > スキーマ設計方針 > 削除戦略を論理削除一律から論理/物理使い分けに修正

### 追加した共通コンポーネント候補（1件）
- FE: app-tree-view（階層ツリー表示、展開/折りたたみ、アクションボタン）

### 更新ファイル
| カテゴリ | 件数 |
|---|---|
| アーキテクチャ設計 | 1（spec/architecture.md 差分更新） |
| ワークログ | 2（spec/worklog/cross-cutting.md 更新、spec/worklog/index.md 更新） |
| トレーサビリティ | 1（spec/traceability/index.md 最終更新日更新） |

### 未確認事項
- カテゴリ並べ替えの永続化要否（現行は非永続化。sort_orderカラム+API追加の要否）
- productCountの累計表示要否（現行は直接属する製品数のみ。子カテゴリ含む累計が必要か）
- カテゴリ管理のロール制限（現行は@RolesAllowed未設定。ADMIN/MANAGERのみに制限すべきか）

### 次に実行すべきskill
- `08-test-data-design`
- next target feature ID: F-03-03
- current 中分類: F-03-03
- 中分類進捗: 3/5 中分類完了（F-03-01 done、F-03-02 done、F-03-03はアーキテクチャ設計完了。F-03-04〜F-03-05は04-ui-specificationから）
- reason: F-03-03のアーキテクチャ設計差分更新完了。次はテストデータ設計

---

## 2026-05-24: Phase 7 - アーキテクチャ基本設計（4回目: F-03-02 差分更新）

### 実施内容
- F-03-02（製品詳細画面）のUI仕様・API仕様・ユースケースを確認し、既存の設計方針で対応できない新パターンを特定
- spec/architecture.md を差分更新（4セクション追加、7設計判断追加、共通コンポーネント4項目追加、1項目更新）

### 新パターン分析結果
| パターン種別 | 確認済みパターン | 対応 |
|---|---|---|
| タブ付き詳細画面 | 7タブ構成（基本情報・仕様・画像・代替品・サプライヤー・ドキュメント・変更履歴）、activeTab state管理 | 詳細画面タブ方針セクション追加、app-tab-panel 共通コンポーネント候補追加 |
| エンティティ変更履歴 | /api/products/{id}/change-log、CREATE/UPDATE/STATUS_CHANGEのタイムライン表示 | エンティティ変更履歴方針セクション追加、ChangeLogService 共通処理追加 |
| サブリソースAPI | /api/products/{id}/change-log, /suppliers, /alternatives の3サブリソース | サブリソースAPI方針セクション追加 |
| ファイル・画像表示 | 画像グリッド表示（メインバッジ・フォールバック）、ドキュメントダウンロード | ファイル・画像表示方針セクション追加 |
| DTOマッピング漏れ | createdAt/updatedAt/notes/unit/leadTimeDays/width/height/depth の8フィールド未含 | DTO/Entity方針にDTOマッピング完全性チェック追記 |
| 代替品の偽実装 | GET /api/products?page=0&size=5→自身除外（専用API未使用、技術的負債） | 正しいGET /api/products/{id}/alternatives に移行方針 |
| JSON文字列パース | specifications がJSON文字列→FEでJSON.parse()→キーバリューテーブル表示 | specifications フィールドの取り扱い方針追記 |

### 主要な設計判断（追加: 7項目）
1. 詳細画面タブ管理: コンポーネントstate（Signal） — URLハッシュフラグメント不採用（ブックマーク不要）
2. エンティティ変更履歴: ドメイン別ChangeLog — 汎用AuditLogとは分離（粒度・用途が異なる）
3. サブリソースAPI: 選択的分離 — 常時必要なデータは含める、タブ固有データは分離
4. 代替品API: 専用GET /api/products/{id}/alternatives — 一般製品リスト流用の技術的負債を解消
5. ファイル配信方式: 静的ファイル（nginx直接） — APIストリーミングはCSVエクスポート専用
6. DTOマッピング検証: MapStruct unmappedTargetPolicy=ERROR — コンパイル時に未マッピング検出
7. サプライヤータブAPI: GET /api/products/{id}/suppliers — BEに存在するがFE未使用の技術的負債を解消

### 追加したセクション（4件）
1. FE構成 > 詳細画面タブ方針
2. FE構成 > ファイル・画像表示方針
3. BE構成 > サブリソースAPI方針
4. BE構成 > エンティティ変更履歴方針

### 追記した既存セクション（1件）
1. BE構成 > DTO/Entity方針 > DTOマッピング完全性（F-03-02で確認）

### 追加した共通コンポーネント候補（4件）
- FE: app-tab-panel（タブパネル、遅延レンダリング対応）、app-image-gallery（画像グリッド表示）
- FE共通処理: fileSize パイプ（ファイルサイズ表示変換）
- BE: ChangeLogService（エンティティ変更履歴の記録・取得）

### 更新した共通コンポーネント候補（1件）
- FE共通処理: fileDownload ユーティリティ — 用途にドキュメントダウンロードを追記

### 更新ファイル
| カテゴリ | 件数 |
|---|---|
| アーキテクチャ設計 | 1（spec/architecture.md 差分更新） |
| ワークログ | 2（spec/worklog/cross-cutting.md 更新、spec/worklog/index.md 更新） |
| トレーサビリティ | 1（spec/traceability/index.md 最終更新日更新） |

### 未確認事項
- 変更履歴が多数ある場合のページング対応（現行はページングなし・全件取得）
- ProductAlternativeエンティティのデータ件数とFE表示の対応
- ドキュメントダウンロードの実ファイル存在確認
- 画像のサムネイル/フルサイズ切替機能の要否
- ロール別の編集・削除ボタン表示制御（VIEWER/BUYERへの制限）

### 次に実行すべきskill
- `08-test-data-design`
- next target feature ID: F-03-02
- current 中分類: F-03-02
- 中分類進捗: 2/5 中分類 done（F-03-01 done、F-03-02はアーキテクチャ設計完了。F-03-03〜F-03-05は04-ui-specificationから）
- reason: F-03-02のアーキテクチャ設計差分更新完了。次はテストデータ設計

---

## 2026-05-24: Phase 7 - アーキテクチャ基本設計（3回目: F-03-01 差分更新）

### 実施内容
- F-03-01（製品一覧画面）のUI仕様・API仕様・ユースケースを確認し、既存の設計方針で対応できない新パターンを特定
- spec/architecture.md を差分更新（3セクション追加、7設計判断追加、共通コンポーネント4項目追加）

### 新パターン分析結果
| パターン種別 | 確認済みパターン | 対応 |
|---|---|---|
| マルチフィルタ一覧画面 | キーワード検索 + カテゴリ/メーカー/ステータスの3ドロップダウン + リセットリンク | 一覧画面フィルタ方針セクション追加、app-filter-bar 共通コンポーネント候補追加 |
| サーバーサイドソート | 現行は sortパラメータが@QueryParam未定義で無視される（技術的負債） | Controller方針にソートパラメータ処理を追記、SortParameterParser 共通処理追加 |
| CSV/エクスポート | クライアント側CSV生成 vs バックエンドCSVエクスポートの二重実装 | CSV/ファイルエクスポート方針セクション追加、CsvExportService 共通処理追加 |
| N+1クエリ | 在庫数取得が製品ごとに個別クエリ | Repository方針にJOIN FETCH/DBビュー方針追記 |
| ステータスラベル不一致 | DISCONTINUED=「廃番」(FE) vs「販売終了」(BE) | バリデーション方針にラベル正規化方針追記 |
| マスターデータフィルタ | メーカーリストを全製品(size=1000)取得→ユニーク抽出（技術的負債） | 専用APIエンドポイント使用方針を策定 |
| インメモリフィルタ | manufacturerIdがDB検索ではなくJava removeIf（技術的負債） | DB WHERE句に移行方針を策定 |

### 主要な設計判断（追加: 7項目）
1. CSVエクスポート方式: バックエンドStreamingOutput — 件数制限なし、フィルタ条件適用可能
2. メーカーフィルタデータソース: 専用APIエンドポイント（/api/master-data/manufacturers） — 全製品取得パターンを廃止
3. サーバーサイドソート: バックエンド実装（PanacheのSort） — sortパラメータ@QueryParam追加、ホワイトリスト検証
4. N+1クエリ解消: JOIN FETCH + DBビュー併用 — 一覧APIは一括取得、レポート系はDBビュー活用
5. ステータスラベル管理: BE一元管理 — Enum.displayName()をAPIレスポンスに含めFEはそれを表示
6. インメモリフィルタ: DB WHERE句に移行 — manufacturerIdフィルタのインメモリ実装を廃止
7. 検索デバウンス: 300ms — テキスト検索のAPI呼び出し頻度抑制

### 追加したセクション（3件）
1. FE構成 > 一覧画面フィルタ方針
2. FE構成 > CSV/ファイルエクスポート方針
3. BE構成 > Repository方針 > N+1クエリの解消

### 追加した共通コンポーネント候補（4件）
- FE: app-filter-bar（一覧画面フィルタバー）、app-export-button（CSVエクスポートボタン）
- FE共通処理: debounceSearch ユーティリティ、fileDownload ユーティリティ
- BE: CsvExportService、SortParameterParser

### 更新ファイル
| カテゴリ | 件数 |
|---|---|
| アーキテクチャ設計 | 1（spec/architecture.md 差分更新） |
| ワークログ | 2（spec/worklog/cross-cutting.md 新規、spec/worklog/index.md 更新） |
| トレーサビリティ | 1（spec/traceability/index.md 最終更新日更新） |

### 未確認事項
- 親カテゴリ選択時の子カテゴリ製品包含動作（現行は完全一致のみ、移行後の方針未決定）
- DRAFTステータスのフィルタ選択肢追加要否
- 検索のデバウンス実装詳細（現行はデバウンスなし）
- CSVエクスポート時の権限制御（本来VIEWERは出力不可等の制限が必要な可能性）

### 次に実行すべきskill
- `08-test-data-design`
- next target feature ID: F-03-01
- current 中分類: F-03-01
- 中分類進捗: 1/5 中分類 done（F-03-01のみ。F-03-02〜F-03-05は04-ui-specificationから）
- reason: F-03-01のアーキテクチャ設計差分更新完了。次はテストデータ設計
