# リグレッション比較レポート: F-04-01 サプライヤー一覧画面

## サマリー
- 対象シナリオ数: 54
- 現行: 54 pass / 0 fail
- 移行後: 50 pass / 4 fail
- 差分: 4件

## 差分一覧

| # | シナリオ | 差分内容 | 現行の挙動 | 移行後の挙動 | 分類 | 対応方針 | 対応先skill |
|---|---|---|---|---|---|---|---|
| 1 | 評価フィルタで「1以上」を選択すると0件になる | 評価フィルタの絞り込み結果が異なる | 全サプライヤーのrating=0.0（SupplierMapperがratingフィールドをignoreするためDTOに反映されない）のため、「1以上」フィルタで0件表示 | ratingが正しく計算・反映されるため、評価1以上のサプライヤーが表示される | 意図的変更 | 受容 | - |
| 2 | 評価が星表示と数値で表示される | SUP-001の評価値が異なる | SUP-001のrating="0.0"（Mapperのignoreにより常に0） | SUP-001のrating="3.8"（SupplierRatingテーブルのoverallScore平均を正しく計算） | 意図的変更 | 受容 | - |
| 3 | メールアドレスと電話番号が表示される | SUP-001のメールアドレスが異なる | メールアドレス=""（enrichDto()でprimary contactを取得するが、contactsリレーションがLazy fetchでLazyInitializationExceptionにより空文字） | メールアドレス="matsumoto.kenta@otsuka-shokai.example.com"（contactsリレーションを正しくEAGER/JOIN FETCHでロード） | 意図的変更 | 受容 | - |
| 4 | 比較画面に発注実績がある | 比較画面の発注実績表示有無 | 比較画面に「発注」「実績」等のテキストが含まれ、発注実績データが表示される | 比較画面はルーティングのみ実装で専用UIコンポーネントが未作成のため、発注実績データが表示されない | 移植漏れの可能性 | 修正 | 11-migrated-implementation |

## 意図的変更の理由

| # | 変更内容 | 理由 |
|---|---|---|
| 1 | 評価フィルタの絞り込み結果 | 現行のSupplierMapperはratingフィールドに@Mapping(target="rating", ignore=true)を設定しており、DBに存在する評価データがDTOに反映されない技術的負債がある。移行後はrating計算を正しく実装し、SupplierRatingテーブルのoverallScore平均値をレスポンスに含めるようにした |
| 2 | 評価値の表示 | 差分#1と同じ理由。現行のrating=0.0は表示上の不具合であり、移行後の値が正しい |
| 3 | メールアドレス・電話番号の表示 | 現行のenrichDto()はcontactsリレーションをLazy fetchで取得するが、トランザクション境界外でアクセスするためLazyInitializationExceptionが発生し空文字となる。移行後はクエリ時にcontactsを適切にJOIN FETCHすることで解消した |

## 未確認事項

- 比較画面の専用UIコンポーネント（SupplierCompareComponent）の詳細仕様: 現行の比較画面がどのようなレイアウト・データ項目を持つかの詳細確認が必要
- 比較画面の発注実績データソース: 現行でどのAPIから発注実績を取得しているかの確認が必要

## 結論
- 差分総数: 4件
- 意図的変更: 3件（rating計算の正常化、contacts データロードの正常化）
- 移植漏れの可能性: 1件（比較画面の専用UIコンポーネント未作成）
- 仕様不明: 0件
- テスト不備: 0件
- 現行バグの可能性: 0件
- 未確認: 0件
